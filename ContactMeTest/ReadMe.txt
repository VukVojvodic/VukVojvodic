Ready? Start the development server by running command in a terminal:

npx serve

Then open up another terminal and run:

npm run e2e

You should see Cypress opening a browser and going through the page