1 zadatak:
var strA = parseInt(window.prompt("Kolika je stranica A? "));
var strB = parseInt(window.prompt("Kolika je stranica B? "));
if(Number.isInteger(strA) === true && Number.isInteger(strB) === true){
var obim = 2 * strA + 2 * strB;
var povrs = strA * strB;
console.log ("Obim tela je " + obim + " a povrsina tela je "+ povrs + "."); 
}else{console.log ("Niste uneli broj!")}

2 zadatak
var broj = parseInt(window.prompt("Broj stepenovanja je?"));
var stepen = parseInt(window.prompt("Na koji stepen?"));
if(Number.isInteger(broj) === true && Number.isInteger(stepen) === true){
var stepenovanje = Math.pow(broj, stepen);
console.log ("Broj " + broj + " na " + stepen +" stepen inosi "+ stepenovanje + "."); 
}else{console.log ("Niste uneli dobre podatke!")}

3 zadatak
var brA = parseInt(window.prompt("Kolika je stranica A? "));
var brB = parseInt(window.prompt("Kolika je stranica B? "));
if(Number.isInteger(brA) === true && Number.isInteger(brB) === true && brA + brB > 10){
var zbir = brA + brB;
console.log("Zbir je " + zbir);
}else if(Number.isInteger(brA) === true && Number.isInteger(brB) === true && brA + brB < 10 && brA > brB){
var razlika = brA - brB;
console.log("Razlika je " + razlika);
}else if(Number.isInteger(brA) === true && Number.isInteger(brB) === true && brA + brB === 10){
console.log("Zbir je 10");
}else{console.log ("Niste uneli broj!")}

4 zadatak
var zbir = 0;
for(var i = 0; i <= 10; i++){
  zbir += i;
}console.log(zbir);

5 zadatak
const arr = [1, 2, 3, 4];
arr.forEach((item) => console.log(item * 2));

6 zadatak
var skup = [5, 6, 8, 10, 4, 20];
function SrednjaVrednost(skupa) {
  var sum = skupa.reduce((a, b) => a + b, 0);
  return Math.round(sum / skupa.length);
}console.log(SrednjaVrednost(skup));

7 zadatak
var brA = parseInt(window.prompt("Broj A?"));
var brB = parseInt(window.prompt("Broj B?"));
var brC = parseInt(window.prompt("Broj C?"));
function SrednjaVrednost(broj) {
  var srv = (brA + brB + brC) / 3;
  return Math.round(srv);
}console.log(SrednjaVrednost(brA, brB, brC));

var brA = parseInt(window.prompt("Broj A?"));
var brB = parseInt(window.prompt("Broj B?"));
var brC = parseInt(window.prompt("Broj C?"));
function SrednjaVrednost(broj) {
  var arr = [];
  arr.push(brA, brB, brC); 
  var srv = arr.reduce(function (a, b) {return a + b;}, 0) / 3;
  return Math.round(srv);
}console.log(SrednjaVrednost(brA, brB, brC));

8 zadatak
var cel = parseInt(window.prompt("Broj Celziusa?"));
function celsiusToFahrenheit(broj) {
  var far = cel * 9 / 5 + 32;
  return far;
}console.log(cel + " Celzijusa je " + celsiusToFahrenheit(cel) + " Farenhajta!");

9 zadatak
var brA = parseInt(window.prompt("Broj A?"));
var brB = parseInt(window.prompt("Broj B?"));
var brC = parseInt(window.prompt("Broj C?"));
var arr = [];
  arr.push(brA, brB, brC);
var n = parseInt(window.prompt("Broj N?"));
arr.forEach((item, i, self) => self[i] = item + n);
console.log(arr);

10 zadatak
var brA = parseInt(window.prompt("Broj A?"));
var brB = parseInt(window.prompt("Broj B?"));
var brC = parseInt(window.prompt("Broj C?"));
var arr = [];
  arr.push(brA, brB, brC);
function sumArray(array) {
  for (
    var
      index = 0,
      length = array.length,
      sum = 0;
      index < length;
      sum += array[index++]
  );
  return sum;
}console.log(sumArray(arr));

11 zadatak
var brA = parseInt(window.prompt("Broj A?"));
var brB = parseInt(window.prompt("Broj B?"));
var brC = parseInt(window.prompt("Broj C?"));
var arr = [];
arr.push(brA, brB, brC);
var broj = parseInt(window.prompt("Broj uporedjivanja?"));
for(i = 0; i < arr.length; i++) {
  if(broj <= arr[i]) {
    console.log(arr[i]);
  }
}